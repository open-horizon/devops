{
    "api_addr": "${HZN_TRANSPORT}://${VAULT_DEV_LISTEN_ADDRESS}",
    "cluster_addr": "http://0.0.0.0:8201",
    "disable_mlock": true,
    "listener": {
        "tcp": {
            "address": "${VAULT_DEV_LISTEN_ADDRESS}",
            "cluster_address": "0.0.0.0:8201",
            "tls_disable": ${BAO_DISABLE_TLS}
        }
    },
    "log_level": "${BAO_LOG_LEVEL}",
    "storage": {
        "file": {
            "path": "/vault/file"
        }
    },
    "ui": false
}
